# Test Fixtures

Store static test data here: sample chapter text, serialized API payloads, and browser-flow seed data.

Keep fixtures deterministic and small. Use `tests/factories` when data needs per-test variation.

Prompt fixtures are LLM-free contract checks. They should pin guardrails, schema rendering, and representative model-output validation behavior without calling a live model.
